import { searchTest as test } from '@/tests/tests';
import { usersAuth } from '@/data/userAuthorization';

// export async function AuthTest(fixture){
//   test(`${fixture.name} - Авторизация с валидными данными`, async (fixture) => {
//     await fixture.authorization(usersAuth.validUser.email, usersAuth.validUser.password);
//   });
// }